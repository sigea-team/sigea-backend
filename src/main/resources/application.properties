spring.application.name=demosigea-backend

# ===================================
# CONEXIÓN A BASE DE DATOS NEON (POSTGRESQL)
# ===================================
spring.datasource.url=jdbc:postgresql://ep-autumn-firefly-b5dumshb-pooler.c-7.us-east-2.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_LrmUFwa69xeB
spring.datasource.driver-class-name=org.postgresql.Driver

# ===================================
# CONFIGURACIÓN DE HIBERNATE / JPA
# ===================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===================================
# CONFIGURACIÓN DE CORREO SMTP (MAILTRAP / MAILHOG)
# ===================================
spring.mail.host=${SPRING_MAIL_HOST:sandbox.smtp.mailtrap.io}
spring.mail.port=${SPRING_MAIL_PORT:2525}
spring.mail.username=${SPRING_MAIL_USERNAME:56d0a8b8311690}
spring.mail.password=${SPRING_MAIL_PASSWORD:009dc8384aab9f}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ===================================
# PARÁMETROS DEL SISTEMA SIGEA
# ===================================
app.mail.from=${APP_MAIL_FROM:no-reply@sigea.com}
app.mail.frontend-url=${APP_FRONTEND_URL:http://localhost:8080}
app.mail.verification-path=/api/v1/auth/verify-email
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration-ms=86400000
app.roles.default-initial=PARTICIPANTE
